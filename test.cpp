#include <bits/stdc++.h>
#include "Protocol.h"

using namespace std;

int main(int argc, char**argv) {
    Protocol p;
    cout << sizeof(p) << " " << sizeof(REQUEST_TYPE) << " " << sizeof(bool) << endl;
    return 0;
}